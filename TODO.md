- [x] Diagnose why CI can’t resolve `sequelize` during `sequelize-cli` run
- [x] Fix test execution so that dependencies in the app image are available
- [x] Ensure `pretest` no longer uses `db:migrate:undo` (which can be brittle in CI)
- [ ] Confirm `make ci` passes end-to-end in GitHub actions logs
- [ ] Re-run `hexlet-check` workflow and ensure it reports success

